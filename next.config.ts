import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the dev-tools indicator so nothing looks out of place during a live demo.
  devIndicators: false,
};

export default nextConfig;
